<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GatherUp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/MiddleBlue</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/White</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView</item>
    </style>
    <style name="Widget.App.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.BottomNavigationView</item>
        <item name="colorPrimary">@color/DarkBlue</item>
    </style>

    <style name="ThemeOverlay.App.BottomNavigationView" parent="">
        <item name="colorPrimary">@color/DarkBlue</item>
    </style>

    <style name="Buttons">
        <item name="backgroundTint">@color/MiddleBlue</item>
        <item name="android:textColor">@color/White</item>
        <item name="textAllCaps">false</item>
        <item name="android:textAlignment">center</item>
        <item name="autoSizeTextType">uniform</item>
    </style>

    <style name="Inputs">
        <item name="backgroundTint">@color/SoftBlue</item>
        <item name="android:textColor">@color/Grey</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="ButtonsFino" parent="Buttons">
        <item name="android:layout_height">35dp</item>
        <item name="android:layout_width">380dp</item>
    </style>

</resources>
